{
  "name": "dcs",
  "private": true,
  "version": "0.0.0",

  "devDependencies": {
    "karma": "~0.10"
  },
  "scripts": {
    "test": "karma start test/unit/karma.conf.js",
    "test-single-run": "karma start test/karma.conf.js  --single-run"
  }
}
