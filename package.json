{
  "name": "dcs",
  "private": true,
  "version": "0.0.0",
  "devDependencies": {
    "karma": "^0.12.16",
    "karma-chrome-launcher": "^0.1.4",
    "karma-jasmine": "^0.1.5",
    "precommit-hook": "latest"
  },
  "scripts": {
    "test": "karma start dcs-app/karma.conf.js",
    "test-single-run": "karma start dcs-app/karma.conf.js  --single-run"
  },
  "precommit": ["test-single-run"]
}
